/var/log/clamd.log {
    olddir /var/log/archiv
    create 640 clamav clamav
    postrotate
        /bin/killall -HUP clamd
    endscript
}

/var/log/clamav.log {
    olddir /var/log/archiv
    create 640 clamav clamav
    postrotate
        /bin/killall -HUP clamd
    endscript
}
